<section class="hero">
  <div class="container">
    <h1>{{ settings.hero_title | default: 'Encuentra la casa de tus sueños' }}</h1>
    <p>{{ settings.hero_subtitle | default: 'Te ayudamos a encontrar la propiedad perfecta' }}</p>
    <a href="#contact-form" class="btn btn--primary">{{ settings.hero_cta_text | default: 'Ver Propiedades' }}</a>
  </div>
</section>

{% schema %}
{
  "name": "Hero",
  "settings": [
    {
      "type": "text",
      "id": "title",
      "label": "Title",
      "default": "Hero Title"
    }
  ]
}
{% endschema %}